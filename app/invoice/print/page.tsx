"use client"

import { Suspense } from "react"
import { useSearchParams } from "next/navigation"

const money = (value: number) => new Intl.NumberFormat("en-US", { style: "currency", currency: "USD" }).format(value)
const date = (value: string) => value ? new Date(value).toLocaleDateString("en-US", { month: "long", day: "numeric", year: "numeric" }) : "—"

function PrintInvoice() {
  const params = useSearchParams()
  const title = params.get("title") || "Cleaning Invoice"
  const total = Number(params.get("total") || 0)
  const due = Number(params.get("due") || 0)
  const status = params.get("status") || "sent"
  return <><style>{`@page{size:letter;margin:0}*{box-sizing:border-box}body{margin:0;background:#eef8f6;color:#132238;font-family:Arial,Helvetica,sans-serif;-webkit-print-color-adjust:exact;print-color-adjust:exact}@media print{body{background:#fff}.no-print{display:none!important}.page{box-shadow:none!important}}`}</style><div className="no-print" style={{ position: "fixed", right: 24, bottom: 24, zIndex: 10, display: "flex", gap: 10 }}><button onClick={() => window.print()} style={button}>Save as PDF</button><button onClick={() => window.close()} style={{ ...button, background: "#e8f1f0", color: "#31505a" }}>Close</button></div><main className="page" style={{ width: "8.5in", minHeight: "11in", margin: "0 auto", padding: "0.46in 0.47in", background: "#fff", boxShadow: "0 12px 40px rgba(19,34,56,.14)" }}><header style={{ display: "flex", justifyContent: "space-between", marginBottom: 30 }}><div style={{ display: "flex", alignItems: "center", gap: 13 }}><div style={{ width: 51, height: 51, borderRadius: 12, display: "grid", placeItems: "center", background: "#008f78", color: "#fff", fontWeight: 800 }}>CQ</div><div><div style={{ fontSize: 25, fontWeight: 800 }}>CleanQuote <span style={{ color: "#008f78" }}>Pro</span></div><div style={{ fontSize: 9, letterSpacing: 1.5, color: "#71808b", fontWeight: 700 }}>PROFESSIONAL CLEANING INVOICE</div></div></div><div style={{ textAlign: "right", fontSize: 10, lineHeight: 1.8, color: "#5c6c7b" }}><strong style={{ color: "#008f78", fontSize: 12 }}>INVOICE #{params.get("invoiceId")}</strong><br/>Issued: {date(new Date().toISOString())}<br/>Status: {status.toUpperCase()}</div></header><section style={{ display: "flex", justifyContent: "space-between", padding: "22px 27px", marginBottom: 25, border: "1px solid #dbece9", borderRadius: 12, background: "rgba(239,249,247,.92)" }}><div><div style={{ fontSize: 26, fontWeight: 800 }}>{title}</div><div style={{ color: "#6b7d88", fontSize: 11, letterSpacing: 1, textTransform: "uppercase" }}>Cleaning service invoice</div></div><div style={{ textAlign: "right" }}><div style={{ color: "#008f78", fontSize: 31, fontWeight: 800 }}>{money(due)}</div><div style={{ color: "#6b7d88", fontSize: 10, fontWeight: 700, letterSpacing: .8 }}>AMOUNT DUE</div></div></section><div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 13, marginBottom: 23 }}><Panel title="BILL TO"><Info label="Name" value={params.get("clientName") || ""}/><Info label="Email" value={params.get("clientEmail") || ""}/><Info label="Phone" value={params.get("clientPhone") || ""}/><Info label="Address" value={params.get("address") || ""}/></Panel><Panel title="INVOICE SUMMARY"><Info label="Invoice total" value={money(total)} right/><Info label="Amount due" value={money(due)} right/><Info label="Due date" value={date(params.get("dueDate") || "")} right/></Panel></div><Panel title="PAYMENT DETAILS"><div style={{ fontSize: 11, color: "#5c6c7b", lineHeight: 1.6 }}>Please remit payment by the due date. Online payment options may be available through your invoice link.</div></Panel>{params.get("notes") && <section style={{ marginTop: 16, padding: "13px 17px", border: "1px solid #dbece9", borderRadius: 10, background: "rgba(239,249,247,.82)" }}><div style={{ color: "#008f78", fontSize: 10, fontWeight: 800, letterSpacing: 1 }}>NOTES</div><div style={{ marginTop: 7, fontSize: 11, color: "#6b7d88" }}>{params.get("notes")}</div></section>}<footer style={{ marginTop: 35, paddingTop: 16, borderTop: "1px solid #dbece9" }}><div style={{ color: "#008f78", fontFamily: "cursive", fontSize: 28, fontStyle: "italic" }}>Thank you!</div><div style={{ marginTop: 5, color: "#71808b", fontSize: 10 }}>We appreciate the opportunity to earn your business.</div></footer></main></>
}
const button = { border: "none", borderRadius: 8, padding: "10px 18px", background: "#008f78", color: "#fff", fontWeight: 700, cursor: "pointer" }
function Panel({ title, children }: { title: string; children: React.ReactNode }) { return <section style={{ padding: "16px 20px", border: "1px solid #dbece9", borderRadius: 10, background: "rgba(255,255,255,.9)" }}><div style={{ marginBottom: 13, color: "#008f78", fontSize: 10, fontWeight: 800, letterSpacing: 1 }}>{title}</div>{children}</section> }
function Info({ label, value, right }: { label: string; value: string; right?: boolean }) { return value ? <div style={{ display: "flex", justifyContent: right ? "space-between" : "flex-start", gap: 12, marginBottom: 9, fontSize: 10.5 }}><span style={{ color: "#71808b" }}>{label}</span><span style={{ fontWeight: 600, textAlign: right ? "right" : "left" }}>{value}</span></div> : null }
export default function Page() { return <Suspense fallback={<div>Loading invoice...</div>}><PrintInvoice /></Suspense> }
