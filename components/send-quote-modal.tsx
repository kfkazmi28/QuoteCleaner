"use client"

import { useEffect, useState } from "react"
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
import { Textarea } from "@/components/ui/textarea"
import { PhoneInput } from "@/components/phone-input"
import { Mail, MessageSquare, FileDown, RefreshCw, Send, Sparkles } from "lucide-react"
import { toast } from "sonner"
import { updateQuote } from "@/app/actions/quotes"
import { getClientContacts } from "@/app/actions/contacts"
import type { ClientContact } from "@/lib/contacts-types"
import { exportQuotePdf } from "@/lib/export-quote-pdf"
import { COMPANY_NAME } from "@/lib/company-config"

export interface SendQuoteData {
  quoteId?: string | null; quoteName: string; selectedTier?: string | null; tierDescription?: string | null; homeVariables?: { squareFootage?: string | number | null; bedrooms?: string | number | null; bathrooms?: string | number | null; pets?: string | number | null; children?: string | number | null }; homeAddress: string; clientName?: string | null; clientEmail?: string | null; clientPhone?: string | null; generatedBy?: string | null; notes?: string | null; resultStandard: number; resultDeepClean: number; resultMoveIn: number; resultMonthly: number; resultBiweekly: number; resultWeekly: number; createdAt?: string; estimatedHours?: number; checklist?: { section: string; items: string[] }[]
}
interface Draft { clientName: string; clientEmail: string; clientPhone: string }
const money = (n: number) => new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 0 }).format(n || 0)


export function SendQuoteModal({ open, onClose, data, onContactSaved }: { open: boolean; onClose: () => void; data: SendQuoteData | null; onContactSaved?: (updated: Partial<SendQuoteData>) => void }) {
  const [contact, setContact] = useState<Draft>({ clientName: "", clientEmail: "", clientPhone: "" })
  const [method, setMethod] = useState<"email" | "text" | "pdf">("email")
  const tone = "Professional"
  const setTone = (_nextTone: string) => undefined
  const [message, setMessage] = useState("")
  const [saving, setSaving] = useState(false)
  const [contacts, setContacts] = useState<ClientContact[]>([])
  const [suggestions, setSuggestions] = useState(false)
  useEffect(() => { if (data) { setContact({ clientName: data.clientName ?? "", clientEmail: data.clientEmail ?? "", clientPhone: data.clientPhone ?? "" }); setMessage(`Hi ${data.clientName || "there"},\n\nThank you for your interest in ${data.quoteName}. Your personalized cleaning quote is ready. We’d be happy to answer any questions and help you schedule your service.\n\nBest,\n${data.generatedBy || COMPANY_NAME}`) } }, [data])
  if (!data) return null
  const selectedPrice = data.quoteName.toLowerCase().includes("deep") ? data.resultDeepClean : data.quoteName.toLowerCase().includes("move") ? data.resultMoveIn : data.resultStandard
  const saveContact = async () => { if (!data.quoteId) return toast.error("Save the quote first to associate client details."); setSaving(true); const result = await updateQuote(data.quoteId, { client_name: contact.clientName || undefined, client_email: contact.clientEmail || undefined, client_phone: contact.clientPhone || undefined }); setSaving(false); if (result.error) toast.error("Could not save client details."); else { toast.success("Client details saved"); onContactSaved?.(contact) } }
  const generate = async () => { setSaving(true); try { const response = await fetch("/api/quote-message", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...data, ...contact, tone }) }); const result = await response.json(); if (!response.ok) throw new Error(); setMessage((result.message || "").replace(/\*+/g, "").replace(/^\s*[-•]\s*/gm, "")) } catch { toast.error("Could not generate a message right now.") } finally { setSaving(false) } }
  const email = () => { if (!contact.clientEmail) return toast.error("Add a client email first."); const url = `mailto:${contact.clientEmail}?subject=${encodeURIComponent(`Your Cleaning Quote – ${data.quoteName}`)}&body=${encodeURIComponent(message)}`; window.open(url, "_blank", "noopener,noreferrer") }
  const text = () => { if (!contact.clientPhone) return toast.error("Add a client phone first."); const url = `sms:${contact.clientPhone}?body=${encodeURIComponent(message)}`; window.open(url, "_blank", "noopener,noreferrer") }
  return <Dialog open={open} onOpenChange={v => !v && onClose()}><DialogContent className="max-h-[92vh] overflow-y-auto p-0 sm:max-w-2xl"><DialogHeader className="border-b border-border px-6 py-6"><DialogTitle className="text-2xl">Share Quote</DialogTitle><DialogDescription><span className="font-medium text-foreground">{data.quoteName} · {money(selectedPrice)}</span><span className="sr-only">Quote sharing options</span></DialogDescription></DialogHeader><div className="flex flex-col gap-7 px-6 py-6"><section className="flex flex-col gap-3"><h3 className="text-sm font-semibold uppercase tracking-[0.14em] text-primary">Client</h3><div className="grid gap-3 sm:grid-cols-2"><div className="flex flex-col gap-1.5"><Label htmlFor="share-name">Client Name</Label><Input id="share-name" value={contact.clientName} onFocus={async () => { if (!contacts.length) setContacts(await getClientContacts()); setSuggestions(true) }} onChange={e => { setContact(c => ({ ...c, clientName: e.target.value })); setSuggestions(true) }} placeholder="Client name" />{suggestions && contact.clientName && contacts.filter(c => c.name.toLowerCase().includes(contact.clientName.toLowerCase())).slice(0, 4).map(c => <button key={c.id} type="button" className="text-left text-sm text-muted-foreground" onClick={() => { setContact({ clientName: c.name, clientEmail: c.email || "", clientPhone: c.phone || "" }); setSuggestions(false) }}>{c.name}</button>)}</div><div className="flex flex-col gap-1.5"><Label htmlFor="share-email">Client Email</Label><Input id="share-email" type="email" value={contact.clientEmail} onChange={e => setContact(c => ({ ...c, clientEmail: e.target.value }))} placeholder="client@example.com" /></div><div className="flex flex-col gap-1.5 sm:col-span-2"><Label htmlFor="share-phone">Client Phone</Label><PhoneInput id="share-phone" value={contact.clientPhone} onChange={v => setContact(c => ({ ...c, clientPhone: v }))} /></div></div><Button type="button" variant="ghost" className="self-start px-0 text-primary" onClick={saveContact} disabled={saving || !data.quoteId}>{saving ? "Saving..." : "Save client details"}</Button></section><section className="flex flex-col gap-3"><h3 className="text-sm font-semibold uppercase tracking-[0.14em] text-primary">Share via</h3><div className="grid gap-2 sm:grid-cols-3">{([{ key: "email", Icon: Mail, title: "Email", desc: "Send a polished quote by email" }, { key: "text", Icon: MessageSquare, title: "Text", desc: "Send a quick quote by text" }, { key: "pdf", Icon: FileDown, title: "PDF", desc: "Create a professional quote PDF" }] as const).map(({ key, Icon, title, desc }) => <button key={key} type="button" onClick={() => setMethod(key)} className={`flex flex-col gap-1 rounded-xl border p-4 text-left transition-colors ${method === key ? "border-primary bg-primary/5" : "border-border hover:bg-muted/40"}`}><Icon className="h-5 w-5 text-primary" /><span className="font-semibold">{title}</span><span className="text-xs leading-5 text-muted-foreground">{desc}</span></button>)}</div></section>{method !== "pdf" ? <section className="flex flex-col gap-3"><div className="flex flex-wrap items-center justify-between gap-3"><h3 className="text-sm font-semibold uppercase tracking-[0.14em] text-primary">Message preview</h3><div className="flex gap-1">{["Professional", "Friendly", "Concise"].map(option => <Button key={option} type="button" size="sm" variant={tone === option ? "secondary" : "ghost"} onClick={() => { setTone(option); void generate() }}>{option}</Button>)}</div></div><Textarea value={message} onChange={e => setMessage(e.target.value)} className="min-h-40 resize-y" /><Button type="button" variant="ghost" className="self-start px-0 text-primary" onClick={generate} disabled={saving}><RefreshCw className="mr-2 h-4 w-4" />Regenerate</Button><Button type="button" onClick={method === "email" ? email : text} className="w-full sm:w-auto sm:self-end">{method === "email" ? <><Send className="mr-2 h-4 w-4" />Send Email →</> : <><MessageSquare className="mr-2 h-4 w-4" />Open Messages →</>}</Button></section> : <section className="flex flex-col gap-4 rounded-xl border border-border p-4"><div className="rounded-lg bg-muted/30 p-5"><p className="font-semibold text-primary">{COMPANY_NAME}</p><p className="mt-4 text-lg font-semibold">{data.quoteName}</p><p className="mt-2 text-3xl font-bold">{money(selectedPrice)}</p><p className="mt-3 text-sm text-muted-foreground">{contact.clientName || "Client"} · {data.homeAddress}</p><p className="mt-3 text-sm text-muted-foreground">Estimated cleaning time: {data.estimatedHours ? `${data.estimatedHours} hours` : "Included in quote"}</p></div><Button type="button" onClick={() => exportQuotePdf({ ...data, clientName: contact.clientName, clientEmail: contact.clientEmail, clientPhone: contact.clientPhone })} className="w-full sm:w-auto sm:self-end"><FileDown className="mr-2 h-4 w-4" />Download PDF ↓</Button></section>}</div></DialogContent></Dialog>
}
